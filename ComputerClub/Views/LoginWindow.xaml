<ui:FluentWindow x:Class="ComputerClub.Views.LoginWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:local="clr-namespace:ComputerClub.Views"
                 xmlns:viewModels="clr-namespace:ComputerClub.ViewModels"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:computerClub="clr-namespace:ComputerClub"
                 xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
                 xmlns:converters="clr-namespace:ComputerClub.Converters"
                 xmlns:controls="clr-namespace:ComputerClub.Views.Controls"
                 mc:Ignorable="d"
                 Title="Авторизация"
                 Height="500"
                 Width="350"
                 ResizeMode="NoResize"
                 d:DataContext="{d:DesignInstance viewModels:LoginWindowViewModel, IsDesignTimeCreatable=True}"
                 Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterScreen"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 ExtendsContentIntoTitleBar="True">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>
    
    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="Loaded">
            <behaviors:InvokeCommandAction Command="{Binding LoadedCommand}"
                                           CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}" />
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Авторизация" ShowMaximize="False" />

        <controls:LoginControl Grid.Row="1"
                               Visibility="{Binding IsLoginMode, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <controls:RegisterControl Grid.Row="1">
            <controls:RegisterControl.Style>
                <Style TargetType="controls:RegisterControl">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoginMode}" Value="False">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </controls:RegisterControl.Style>
        </controls:RegisterControl>
    </Grid>

</ui:FluentWindow>