<UserControl x:Class="ComputerClub.Views.ManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ComputerClub.Views"
             xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:viewModels="clr-namespace:ComputerClub.ViewModels"
             xmlns:computerClub="clr-namespace:ComputerClub"
             xmlns:converters="clr-namespace:ComputerClub.Converters"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance Type=viewModels:ManagementViewModel, IsDesignTimeCreatable=True}"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <UserControl.Resources>
        <converters:PcTypeToBrushConverter x:Key="PcTypeToBrushConverter" />
        <FrameworkElement x:Key="ProxyElement" DataContext="{Binding}" />
    </UserControl.Resources>

    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="Loaded">
            <behaviors:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>

    <DockPanel>
        <ContentControl Visibility="Collapsed" Content="{StaticResource ProxyElement}" />
        <StackPanel DockPanel.Dock="Right" Width="250">
            <Button Content="Добавить ПК" Command="{Binding AddPcCommand}" Margin="5" HorizontalAlignment="Stretch" />
        </StackPanel>
        <Canvas Background="#2d2d2d" x:Name="RootCanvas">
            <ItemsControl ItemsSource="{Binding PcItems}" Width="{Binding ActualWidth, ElementName=RootCanvas}"
                          Height="{Binding ActualHeight, ElementName=RootCanvas}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left"
                                Value="{Binding X, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <Setter Property="Canvas.Top"
                                Value="{Binding Y, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="1"
                                CornerRadius="6"
                                Background="#6c6c6c"
                                Width="100"
                                Height="70">
        
                            <Border.ContextMenu>
                                <ContextMenu>
                                    <!-- ~1~ Тип ПК @1@ -->
                                    <!-- <MenuItem Header="Тип" ItemsSource="{Binding TypeOptions}"> -->
                                    <!--     <MenuItem.ItemContainerStyle> -->
                                    <!--         <Style TargetType="MenuItem"> -->
                                    <!--             <Setter Property="Header" Value="{Binding Label}" /> -->
                                    <!--             <Setter Property="Command" -->
                                    <!--                     Value="{Binding DataContext.SetPcTypeCommand, -->
                                    <!--         RelativeSource={RelativeSource AncestorType=UserControl}}" /> -->
                                    <!--             <Setter Property="CommandParameter" Value="{Binding}" /> -->
                                    <!--         </Style> -->
                                    <!--     </MenuItem.ItemContainerStyle> -->
                                    <!-- </MenuItem> -->
                                    <!-- -->
                                    <!-- <Separator /> -->

                                    <!-- Удалить -->
                                    <MenuItem Header="Удалить"
                                              Command="{Binding DataContext.RemovePcCommand,
                                            Source={StaticResource ProxyElement}}"
                                              CommandParameter="{Binding}" />
                                </ContextMenu>
                            </Border.ContextMenu>

                            <behaviors:Interaction.Behaviors>
                                <computerClub:DragBehavior GridSizeX="100" GridSizeY="70" />
                            </behaviors:Interaction.Behaviors>

                            <Grid>
                                <Border Height="3"
                                        Background="{Binding TypeId, Converter={StaticResource PcTypeToBrushConverter}}"
                                        VerticalAlignment="Top"
                                        CornerRadius="6,6,0,0" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>
    </DockPanel>

</UserControl>