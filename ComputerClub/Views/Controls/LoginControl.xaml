<UserControl x:Class="ComputerClub.Views.Controls.LoginControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:ComputerClub.Converters"
             xmlns:viewModels="clr-namespace:ComputerClub.ViewModels"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <UserControl.Resources>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
    </UserControl.Resources>

    <Grid Margin="30,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Вход" FontSize="20" FontWeight="SemiBold"
                   HorizontalAlignment="Center" Margin="0,0,0,16" />

        <TextBlock Grid.Row="1" Text="Логин:" Margin="0,0,0,5" />
        <ui:TextBox Grid.Row="2"
                    Text="{Binding Login, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                    Margin="0,0,0,5" />
        <TextBlock Grid.Row="3" Text="{Binding LoginError}" Foreground="Red" FontSize="12"
                   Visibility="{Binding HasLoginError, Converter={StaticResource BooleanToVisibilityConverter}}"
                   Margin="0,0,0,8" />

        <TextBlock Grid.Row="4" Text="Пароль:" Margin="0,0,0,5" />
        <ui:PasswordBox x:Name="PasswordBox"
                        Password="{Binding Password, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                        Grid.Row="5"
                        PasswordChanged="PasswordBox_PasswordChanged"
                        Margin="0,0,0,5" />
        <TextBlock Grid.Row="6" Text="{Binding PasswordError}" Foreground="Red" FontSize="12"
                   Visibility="{Binding HasPasswordError, Converter={StaticResource BooleanToVisibilityConverter}}"
                   Margin="0,0,0,8" />

        <StackPanel Grid.Row="7" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0">
            <ui:Button Content="Войти" Width="150"
                       HorizontalAlignment="Center"
                       Command="{Binding LoginCommand}"
                       CommandParameter="{Binding ElementName=PasswordBox}"
                       IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" />

            <Button Content="Нет аккаунта? Зарегистрироваться"
                    Background="Transparent" BorderThickness="0"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    FontSize="12" Margin="0,8,0,0"
                    Command="{Binding ShowRegisterCommand}" />

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red"
                       TextWrapping="Wrap" Margin="0,10,0,0"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />

            <ProgressBar Height="5" IsIndeterminate="True" Margin="0,10,0,0"
                         Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </StackPanel>
    </Grid>
</UserControl>