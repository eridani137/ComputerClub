<UserControl x:Class="ComputerClub.Views.Controls.ComputerStatusLegendControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:computerClub="clr-namespace:ComputerClub"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <StackPanel Margin="5">
        <TextBlock Text="Статусы компьютеров:"
                   FontWeight="Bold"
                   Margin="0,0,0,6"
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

        <ItemsControl ItemsSource="{x:Static computerClub:ComputerStatuses.All}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <DockPanel Margin="4,2">
                        <Border Width="20" Height="20"
                                Background="{Binding Color}"
                                CornerRadius="3"
                                Margin="0,0,6,0"
                                DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding Name}"
                                   VerticalAlignment="Center"
                                   FontSize="13"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    </DockPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>