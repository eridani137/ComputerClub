<Page x:Class="ComputerClub.Views.Pages.ClientSessionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ComputerClub.Views.Pages"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:pages="clr-namespace:ComputerClub.ViewModels.Pages"
      xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="1200"
      d:DataContext="{d:DesignInstance Type=pages:ClientSessionViewModel, IsDesignTimeCreatable=False}"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="Loaded">
            <behaviors:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>

    <Grid>

        <Grid Visibility="{Binding HasActiveSession, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="1"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       FontSize="18">
                <Run Text="Компьютер №" />
                <Run Text="{Binding ComputerId}" />
            </TextBlock>

            <TextBlock Grid.Row="2"
                       Text="{Binding TimeDisplay}"
                       HorizontalAlignment="Center"
                       FontSize="120"
                       FontWeight="Bold"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       Margin="0,16" />

            <StackPanel Grid.Row="3"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal">
                <TextBlock Text="{Binding TariffName}"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           FontSize="18" />
                <TextBlock Text=" · "
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           FontSize="18" />
                <TextBlock Text="{Binding TotalCost, StringFormat={}{0:N2} ₽}"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           FontSize="18" />
            </StackPanel>

        </Grid>

        <Grid>
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasActiveSession}" Value="False">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <ui:SymbolIcon Symbol="Desktop24"
                               FontSize="64"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               Margin="0,0,0,24" />

                <TextBlock Text="Нет активной сессии"
                           FontSize="24"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Margin="0,0,0,8" />

                <TextBlock Text="Обратитесь к администратору или начните сессию самостоятельно"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,32" />

                <ui:Button Content="Начать сессию"
                           Icon="{ui:SymbolIcon Play24}"
                           Appearance="Primary"
                           FontSize="16"
                           Padding="32,12"
                           HorizontalAlignment="Center"
                           Command="{Binding StartSessionCommand}" />
            </StackPanel>
        </Grid>

    </Grid>

</Page>