<UserControl x:Class="ComputerClub.Views.Pages.ManagementPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ComputerClub.Views"
             xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:viewModels="clr-namespace:ComputerClub.ViewModels"
             xmlns:computerClub="clr-namespace:ComputerClub"
             xmlns:converters="clr-namespace:ComputerClub.Converters"
             xmlns:behaviors1="clr-namespace:ComputerClub.Behaviors"
             xmlns:pages="clr-namespace:ComputerClub.ViewModels.Pages"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance Type=pages:ManagementViewModel, IsDesignTimeCreatable=True}"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <UserControl.Resources>
        <converters:PcTypeToBrushConverter x:Key="PcTypeToBrushConverter" />
    </UserControl.Resources>

    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="Loaded">
            <behaviors:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>

    <DockPanel>
        <StackPanel DockPanel.Dock="Right" Width="250">
            <Button Content="Добавить ПК" Command="{Binding AddPcCommand}" Margin="5" HorizontalAlignment="Stretch" />
        </StackPanel>
        <ScrollViewer HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding PcItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Width="1000"
                                Height="850"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left"
                                Value="{Binding X, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <Setter Property="Canvas.Top"
                                Value="{Binding Y, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="1"
                                CornerRadius="6"
                                Background="#6c6c6c"
                                Width="100"
                                Height="70"
                                Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">

                            <Border.ContextMenu>
                                <ContextMenu>

                                    <MenuItem Header="Изменить тип"
                                              behaviors1:SmartSubmenuBehavior.Enabled="True"
                                              ItemsSource="{Binding PlacementTarget.DataContext.TypeOptions,
                                            RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                        <MenuItem.Icon>
                                            <ui:SymbolIcon Symbol="Laptop24" />
                                        </MenuItem.Icon>

                                        <MenuItem.ItemContainerStyle>
                                            <Style TargetType="MenuItem">
                                                <Setter Property="Header" Value="{Binding Label}" />
                                                <Setter Property="Command"
                                                        Value="{Binding PlacementTarget.Tag.SetPcTypeCommand,
                                                RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                                <Setter Property="CommandParameter" Value="{Binding}" />
                                            </Style>
                                        </MenuItem.ItemContainerStyle>
                                    </MenuItem>

                                    <Separator />

                                    <MenuItem Header="Удалить"
                                              Command="{Binding PlacementTarget.Tag.RemovePcCommand,
                                        RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.DataContext,
                                                RelativeSource={RelativeSource AncestorType=ContextMenu}}">

                                        <MenuItem.Icon>
                                            <ui:SymbolIcon Symbol="Delete24"
                                                           Foreground="{DynamicResource SystemFillColorCriticalBrush}" />
                                        </MenuItem.Icon>

                                    </MenuItem>

                                </ContextMenu>
                            </Border.ContextMenu>

                            <behaviors:Interaction.Behaviors>
                                <behaviors1:DragBehavior GridSizeX="100" GridSizeY="70" />
                            </behaviors:Interaction.Behaviors>

                            <Grid>
                                <Border Height="3"
                                        Background="{Binding TypeId, Converter={StaticResource PcTypeToBrushConverter}}"
                                        VerticalAlignment="Top"
                                        CornerRadius="6,6,0,0" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>

</UserControl>